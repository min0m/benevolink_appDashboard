This app is designed for the existing phpMyAdmin/XAMPP database named benevolink.
It does not create a replacement SQLite database.
It uses live tables and views such as users, missions, applications, events, associations, notifications, v_platform_dashboard, 
v_public_missions, and v_member_impact when available.
